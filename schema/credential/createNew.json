{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "POST /management/credentials",
    "type": "object",
    "properties": {
        "status": {
          "description": "Response status flag",
          "type": "boolean"
        },
        "data": {
            "description": "Credential response payload",
            "type": "object",
            "$ref": "credentialEntity.json"
        }
    },
    "required": [
        "status",
        "data"
    ]
}
