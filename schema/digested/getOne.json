{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "GET /profiles/{userName}/sources/{sourceId}/digested/{digestedName}",
    "type": "object",
    "properties": {
        "status": {
          "description": "Response status flag",
          "type": "boolean"
        },
        "data": {
            "description": "Digested response payload",
            "type": "object",
            "$ref": "digestedEntity.json"
        },
        "updated": {
            "description": "Unixtime of the last update",
            "type": "integer"
        }
    },
    "required": [
        "status",
        "data"
    ]
}
