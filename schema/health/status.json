{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "GET /health/status",
    "type": "object",
    "properties": {
        "status": {
          "description": "Response status flag",
          "type": "boolean"
        },
        "opcache": {
          "description": "Opcode Cache status",
          "type": "array"
        },
        "cache": {
          "description": "Cache status",
          "type": "array"
        }
    },
    "required": [
        "status",
        "opcache",
        "cache"
    ]
}
