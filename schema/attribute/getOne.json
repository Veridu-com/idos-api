{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "GET /profiles/{userName}/attribute/{attributeName}",
    "type": "object",
    "properties": {
        "status": {
          "description": "Response status flag",
          "type": "boolean"
        },
        "data": {
            "description": "Attribute response payload",
            "type": "object",
            "$ref": "attributeEntity.json"
        },
        "updated": {
            "description": "Unixtime of the last update",
            "type": "integer"
        }
    },
    "required": [
        "status",
        "data"
    ]
}
